--!:name
--My game

print("hello world!")

function boot()
    print("hello?")
end


function tick()
    -- Draw a random tile at a random position with random colors
    tile(
        rnd(0, 15), rnd(0, 15), -- Random position
        char(rnd(0x00, 0xff)),   -- Random char
        rnd(0, 15), rnd(0, 15)) -- Random colors
end

-- Rename this file to memo.memo to test auto boot